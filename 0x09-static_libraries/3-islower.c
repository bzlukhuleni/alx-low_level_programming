#include "main.h"

/**
 * check_if_lower - checks if a character is lowercase
 * @character: the character to check
 *
 * Return: 1 if @character is lowercase, 0 otherwise
 */
int check_if_lower(int character)
{
	return (character >= 'a' && character <= 'z');
}
